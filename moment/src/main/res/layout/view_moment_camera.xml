<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextureView
        android:id="@+id/texture_camera"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <wang.leal.moment.view.ProgressView
        android:id="@+id/pv_action"
        android:layout_width="112dp"
        android:layout_height="112dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="61dp"/>

    <ImageView
        android:id="@+id/iv_lock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_camera_record_lock"
        app:layout_constraintTop_toTopOf="@id/pv_action"
        app:layout_constraintBottom_toBottomOf="@id/pv_action"
        app:layout_constraintEnd_toStartOf="@id/pv_action"
        android:layout_marginEnd="20dp"
        android:visibility="gone"
        />
</merge>